name: "Build"

on:
  workflow_call:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  build_amd64:
    name: "Build AMD64"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: "Set up QEMU"
        uses: docker/setup-qemu-action@v1
      - name: "Set up Docker Buildx"
        uses: docker/setup-buildx-action@v1
      - name: "Build Docker Image"
        uses: docker/build-push-action@v2
        with:
          context: .
          platforms: linux/amd64
          tags: amp-dockerized:latest
          outputs: type=docker,dest=/tmp/amp-dockerized-amd64.tar
      - name: "Upload Docker Image Artifacts"
        if: ${{ github.event_name == 'workflow_call' }}
        uses: actions/upload-artifact@v2
        with:
          name: image-amd64
          path: /tmp/amp-dockerized-amd64.tar

  build_arm64:
    name: "Build ARM64"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: "Set up QEMU"
        uses: docker/setup-qemu-action@v1
      - name: "Set up Docker Buildx"
        uses: docker/setup-buildx-action@v1
      - name: "Build Docker Image"
        uses: docker/build-push-action@v2
        with:
          context: .
          platforms: linux/arm64
          tags: amp-dockerized:latest
          outputs: type=docker,dest=/tmp/amp-dockerized-arm64.tar
      - name: "Upload Docker Image Artifacts"
        if: ${{ github.event_name == 'workflow_call' }}
        uses: actions/upload-artifact@v2
        with:
          name: image-arm64
          path: /tmp/amp-dockerized-arm64.tar
